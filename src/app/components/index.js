import BagGlobalFormCreate from "@/app/components/GlobalFormCreate.vue";

const setupComponents = (app) => {
    app.component('BagGlobalFormCreate', BagGlobalFormCreate)
}

export default setupComponents
